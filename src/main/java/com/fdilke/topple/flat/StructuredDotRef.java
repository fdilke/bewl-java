package com.fdilke.topple.flat;

/**
 * Tag interface for types that represent a dot with additional structure e.g. a product
 */
public interface StructuredDotRef { // tag interface; no content required
}
